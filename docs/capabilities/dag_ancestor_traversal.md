# Accelerate lowest common ancestor lookups in commit graphs

Speed up merge-base calculations across long-lived development branches.


## Verification
- Test ancestor lookup performance on complex merge branches
