# Compute maximum and average dependency path lengths

Identify excessively deep dependency chains that increase build vulnerability.


## Verification
- Add assertions for transitive dependency chain depth
