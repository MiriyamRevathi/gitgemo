# Determine blast radius for modified interface definitions

Gauge downstream breakage potential when central data types are edited.


## Verification
- Verify impact radius calculation on deeply nested components
