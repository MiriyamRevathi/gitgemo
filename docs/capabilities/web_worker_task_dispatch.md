# Implement worker pool for intensive graph computations

Offload heavy topological sorting and layout physics to worker threads.


## Verification
- Verify graceful worker thread termination on unmount
